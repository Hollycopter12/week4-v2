
$(function() {

  // Write a function that yells (e.g. TACOS!!!!!)
  var yell = function(string) {
    // change this so it works
    // .toUpperCase() is a built-in function
    return string.toUpperCase() + "!!!!!"
  }

  var DogSaysWhat = "Woof"

  window.alert(yell(DogSaysWhat))
})
